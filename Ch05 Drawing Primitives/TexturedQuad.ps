Texture2D colorTexture : register(t0);
SamplerState colorSampler : register(s0);

struct PSIn
{
    float4 position : SV_Position;
    float2 tex : TEXCOORD;
};

float4 main(PSIn psIn) : SV_Target
{
    float4 color = colorTexture.Sample(colorSampler, psIn.tex);
    return color;
}
